{
  "name": "budget-zero-web",
  "version": "1.0.0",
  "description": "Budget Zero Frontend",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist",
    "generate:sdk": "cd ../backend && make generate",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@vitest/ui": "^3.2.4",
    "jsdom": "^26.1.0",
    "typescript": "^5.6.2",
    "vite": "^5.4.10",
    "vitest": "^3.2.4"
  }
}
