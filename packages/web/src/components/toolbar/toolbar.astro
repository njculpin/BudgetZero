---
/**
 * The row of controls that sits between a page header and its results:
 * a sort control, a set of filters. It stacks on a phone.
 */
import './toolbar.css';
---

<div class="toolbar">
  <slot />
</div>
