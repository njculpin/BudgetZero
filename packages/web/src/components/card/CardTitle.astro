---
const { className = "" } = Astro.props;
---

<h3 class={`card__title ${className}`}>
  <slot />
</h3>

<style>
.card__title {
  font-size: var(--text-lg);
  font-weight: var(--font-weight-semibold);
  line-height: var(--leading-tight);
  margin: 0;
}
</style>
