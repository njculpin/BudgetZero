---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div class={`tag-list ${className || ''}`}>
  <slot />
</div>

<style>
.tag-list {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
}
</style>
