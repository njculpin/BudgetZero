---
interface Props {
  variant: 'draft' | 'private' | 'public' | 'published' | 'archived' | 'primary' | 'secondary' | 'muted' | 'success' | 'warning' | 'info' | 'error';
  size?: 'sm' | 'md' | 'lg';
}

const { variant, size = 'md' } = Astro.props;
---

<span class={`badge badge--${variant} badge--${size}`}>
  <slot />
</span>

<style>
.badge {
  display: inline-block;
  padding: var(--spacing-xxs) var(--spacing-lg);
  font-weight: var(--font-weight-semibold);
  border-radius: var(--radius-full);
  text-transform: uppercase;
  line-height: var(--leading-tight);
}

.badge--sm {
  font-size: var(--text-xs);
  padding: var(--spacing-xxs) var(--spacing-md);
}

.badge--md {
  font-size: var(--text-xs);
}

.badge--lg {
  font-size: var(--text-sm);
  padding: var(--spacing-xs) var(--spacing-xl);
}

/* Status Variants */
.badge--draft {
  background-color: var(--warning-bg);
  color: var(--warning-fg);
}

.badge--private {
  background-color: oklch(92% 0.03 280 / 1);
  color: oklch(40% 0.12 280 / 1);
}

.badge--public {
  background-color: var(--success-bg);
  color: var(--success-fg);
}

.badge--published {
  background-color: var(--success-bg);
  color: var(--success-fg);
}

.badge--archived {
  background-color: var(--muted);
  color: var(--muted-foreground);
}

/* Semantic Variants */
.badge--success {
  background-color: var(--success-bg);
  color: var(--success-fg);
}

.badge--warning {
  background-color: var(--warning-bg);
  color: var(--warning-fg);
}

.badge--info {
  background-color: var(--info-bg);
  color: var(--info-fg);
}

.badge--error {
  background-color: var(--error-bg);
  color: var(--error-fg);
}

/* Theme Variants */
.badge--primary {
  background-color: var(--primary);
  color: var(--primary-foreground);
}

.badge--secondary {
  background-color: var(--secondary);
  color: var(--secondary-foreground);
}

.badge--muted {
  background-color: var(--muted);
  color: var(--muted-foreground);
}
</style>
